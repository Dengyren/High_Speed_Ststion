@model DAL.View_UserInfo

@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Edit</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>View_UserInfo</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.车牌号)

        @Html.HiddenFor(model => model.身份证号码)

        @Html.HiddenFor(model => model.姓名)

        @Html.HiddenFor(model => model.密码)

        @Html.HiddenFor(model => model.手机号码)

        @Html.HiddenFor(model => model.籍贯)

        @Html.HiddenFor(model => model.驾驶证号码)

        <div class="form-group">
            @Html.LabelFor(model => model.身份证照片, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.身份证照片, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.身份证照片, "", new { @class = "text-danger" })
            </div>
        </div>


        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
